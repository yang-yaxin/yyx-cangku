spring:
  cloud:
    config:
      name: provider-contract
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
      uri: http://localhost:8766/

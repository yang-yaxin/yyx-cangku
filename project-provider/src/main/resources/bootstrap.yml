spring:
  cloud:
    config:
      name: project-provider
      label: master
      profile: dev
      discovery:
        enabled: true
        service-id: config-server
      uri: http://localhost:8766/
    bus:
      enabled: true
      trace:
        enabled: true
management:
  endpoint:
    web:
      exposure:
        include: "*"